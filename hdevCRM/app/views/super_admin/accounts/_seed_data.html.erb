<% if ENV.fetch('ENABLE_ACCOUNT_SEEDING', !Rails.env.production?)  %>
<section class="main-content__body">
 <hr/>
  <%= form_for([:seed, namespace, page.resource], method: :post, html: { class: "form" }) do |f| %>

  <div class="form-actions">
    <div class="pb-3">
      <p>Clique no botão pra gerar dados de demonstração nesta conta.</p>
      <p class="text-color-red">Atenção: isso apaga todos os dados existentes desta conta.</p>
    </div>
  <%= f.submit 'Gerar dados de demonstração' %>
  </div>
  <% end %>
</section>
<% end %>
