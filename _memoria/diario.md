# Diário de sessões

> Continuidade entre sessões. O `/salvar` registra aqui uma linha do que
> foi feito; o `/abrir` lê as últimas entradas pra retomar de onde parou.
> Formato: `- AAAA-MM-DD — o que foi feito (1 linha)`. Mais recente no topo.

- 2026-08-06 — **F8 (conexões de IA, catálogo, gate por plano): DESENHADA E PLANEJADA — pronta pra executar.** Design aprovado com três decisões do Harvey: (1) **gate estrito + seed** — `plan_ai_models` vazio = plano não libera nada; a migration seeda todos os planos × todos os modelos pra ninguém mudar de comportamento no deploy; (2) **runtime degrada com log** — modelo fora do plano cai pro default liberado (`Ai::ModelNotAllowedError` herda de `QuotaExceededError` DE PROPÓSITO: os 3 call sites que resgatam quota silenciam a IA de graça); (3) **credencial só no super admin** — cliente nenhum cadastra chave; `api_key` nullable com fallback pro `GlobalConfigService` até o Harvey cadastrar no painel. Sacadas do plano: preço em **cents inteiros/1M** torna o custo múltiplo exato de 1e-8 (o "arredondar pra cima" evapora); seeds de migration em **SQL puro** (nenhum valor cifrado — a conexão nasce com chave NULL); o catálogo seedado no banco de teste faz o `pricing_spec` atual passar sem edição; o seed cartesiano de `plan_ai_models` NÃO é testável no CI (banco de teste migra sem planos) — prova é SELECT em produção. SDK Ruby confirmado: `Anthropic::BedrockMantleClient` existe, Bedrock usa prefixo `anthropic.` no model id — valida a separação canonical/provider. Design: `docs/superpowers/specs/2026-08-06-f8-conexoes-ia-design.md`; plano (10 tasks): `docs/superpowers/plans/2026-08-06-f8-conexoes-ia.md`. Pendência F7.5: rerun do lint do run 31116957671 (falha de INFRA no "Set up job", rspec verde) disparado; conferir verde.
- 2026-08-06 — **F7.5 (quota por contador atômico): MERGEADA — PR #35, CI verde 4/4 no espelho (run 31115556750), `SUM()` fora do hot path da IA.** A tabela `ai_usage_counters` (dono polimórfico Account|Agency, unique por dono+mês) é incrementada pelo `after_create` de `AiUsageEvent` com `update_counters` — mesmo padrão atômico do `AiCreditEvent.record!`; corrida de criação decidida pelo índice único (`find_or_create_by!` + rescue `RecordNotUnique` + retry — nunca `create_or_find_by!`, armadilha da F7). `Ai::QuotaService#account_usage`/`#agency_usage` viraram lookup O(1) — o ToolLoop consulta `exceeded?` até 8× por resposta e cada consulta era um `SUM(total_tokens)`. O mailer saiu da request: `check_thresholds!` intacto (cooldown Redis 24h), mas agora chamado pelo `Ai::QuotaAlertJob` enfileirado em `after_create_commit`. **Dois desvios do plano, ambos deliberados:** custo em `decimal(14,8)` (o plano pedia `cost_cents bigint` — evento de Haiku custa ~0.045 centavo, arredondar por evento zeraria o acumulado que é o número de margem da F11) e **backfill dentro da migration** (sem ele, deploy no meio do mês zera o consumo aparente e quota estourada volta a gastar). Spec de mês anterior trocou `update_columns` pós-create por `travel_to` na criação (o update burlaria o callback do contador). Rubocop round 1: compact class no job (o `reply_job` aninhado está congelado no todo — gate vale pro novo), backfill extraído em 2 métodos privados (`MethodLength` 25/19), matcher sem linha de continuação. **Pendente de produção: no próximo deploy a migration roda no boot; conferir no terminal EasyPanel que contador == SUM por dono (`SELECT` de sanidade do backfill).** Design/plano em `docs/superpowers/{specs,plans}/2026-08-06-f75-*`. Próxima: F8 (conexões de IA, catálogo, gate por plano — desenho ANTES de código, 4 tabelas).
- 2026-08-06 — **F7 (Stripe): o ciclo do dinheiro fechou em código — PR #34, CI verde 4/4 no espelho (run 31104702530, 6452 exemplos / 0 falhas), API do Stripe ainda NÃO conectada.** `Subscription#activate!`, `#mark_past_due!` e `#cancel!`, que estavam no repo **sem um único chamador**, passaram a ser dirigidos pelo `Webhooks::StripeController` (HMAC via `Stripe::Webhook.construct_event`, que também dá a janela anti-replay) através do `StripeBilling::EventHandler`. **A idempotência foi desenhada pra fechar contra corrida E contra crash**: `find_or_create_by!` + `with_lock` + re-checagem dentro do lock, com mutação e `mark_processed!` na **mesma transação** — o padrão comum ("cria, rescue duplicado, 200") engoliria o retry pós-crash e o cliente pagaria sem receber acesso. Evento órfão vira status `ignored` (novo, sem migration) + **200 deliberado** (4xx faria o Stripe retentar 3 dias um evento que nunca casa). Entrou o Customer Portal e, com ele, o 5º evento `customer.subscription.updated` — sem ele a troca de plano no portal mudaria no Stripe e os limites daqui ficariam velhos. **Regra de negócio do Harvey, aplicada no backend e não só escondendo botão:** só dono de agência e conta direta veem Stripe; conta-filha de agência recebe **403** (paga a agência por fora). E os controllers de billing **pulam os guards de suspensão de propósito** — agência `pending_payment` no 1º pagamento e dono suspenso por inadimplência **precisam** alcançar o checkout, senão pagar seria impossível justamente pra quem precisa. Checkout recusa (422) quem já tem plano vigente (senão dava pra abrir checkout caro, abandonar, e ficar com os limites dele — `grants_plan?` olha status, não pagamento). **Conferir a doc oficial em vez de copiar exemplo pagou:** três campos mudaram de lugar na API atual — `invoice.parent.subscription_details.subscription` (o `invoice.subscription` de topo sumiu), `lines.data[].period.end` (não `invoice.period_end`) e `items.data[].current_period_end` (saiu do topo do Subscription). **O round 1 do CI pegou um bug de produção real:** `create_or_find_by!` não serve com validação de uniqueness no model — ele cria primeiro, então **todo replay do Stripe dava 500 em vez de 200**, e o Stripe retentaria em loop; virou `find_or_create_by!` com rescue da corrida. (Outra do round: `show_exceptions = true` no test env transforma `RoutingError` em 404, então spec que espera a exceção falha.) Sem migration nessa fase. **Falta ligar a conta Stripe** — roteiro em checkbox, do zero, em `docs/superpowers/specs/2026-08-06-f7-stripe-testes-pendentes.md`; a UI de upgrade também ficou pra depois (hoje é só API).
- 2026-08-06 — **F6 comercial (a do plano F6–F11, NÃO a do Motor): os limites de plano viraram regra de negócio, e o ciclo fechou até produção.** PR #33 (`5198106`) mergeado e **migrations aplicadas no container**. `Plan::LimitEnforcer` virou o único lugar que responde "pode?", resolvendo **por chave** — alocação da agência (chave presente vence; `null` = ilimitado explícito) → assinatura direta vigente (decide sozinha, não cai pro degrau seguinte) → plano da agência (**por conta**, decisão do Harvey; carve-up explícito só via `plan_allocations`) → nada = ilimitado. Cinco pontos consultando: criação de inbox (caixas + teto por tipo de canal + instância baileys, guard rodando **antes** do canal nascer, então nenhum HTTP externo acontece), `Account#usage_limits` (o `validate_limit` do agents_controller ficou intacto — só trocou a fonte, como o reconhecimento previa), criação de conta-filha e o `Ai::QuotaService`. Duas colunas jsonb (`plans.channel_limits`, `accounts.plan_allocations`) + CRUD em `/super_admin/plans` com `channel_limits` editado como JSON via acessor virtual validado. **CI: rspec/vitest/baileys verdes DE PRIMEIRA nas 3 rodadas; os 2 vermelhos foram só rubocop** — `Metrics/ClassLength` (o `inboxes_controller` já estava em 175/175 **antes** da F6; virou o concern `PlanLimitGuard` + endless method) e, na rodada 2, `Rails/LexicallyScopedActionFilter`: **before_action dentro de concern referenciando action do controller reprova** (o `WhatsappHealthManagement` passa porque define as próprias actions) — o filtro voltou pro controller. Verde 4/4 no run 31058720729. **Duas armadilhas de operação que custaram diagnóstico:** (1) o `db:migrate` manual pós-deploy sai **mudo** porque o entrypoint já migrou no boot — silêncio ali significa "nada pendente", NÃO "imagem velha", e quem responde é o `db:migrate:status`; (2) os checks do PR no repo **privado** ficam vermelhos falhando em ~4s **sem log algum** — é o billing bloqueado, não teste (quem valida é o espelho; `main` não tem branch protection). Prova final no container: colunas presentes e `Plan::LimitEnforcer` **carregando** (o Zeitwerk resolveu a constante aninhada sob um model ActiveRecord — era o último risco estrutural) e devolvendo `nil` pra conta sem plano. **Nada muda de comportamento até o primeiro plano ser criado no painel. Próximo: F7 (Stripe) — `Subscription#activate!`/`#mark_past_due!`/`#cancel!` seguem sem um único chamador.**
- 2026-08-05 (tarde) — **Fase 6 (disparo em massa) — a ÚLTIMA do Motor — com código completo, PR #32 aberto e CI verde 4/4 DE PRIMEIRA** (run 31026809070 no espelho; primeira fase do motor sem nenhum fix round — specs Ruby às cegas E lint limpos de estreia). Duas decisões do Harvey mudaram o escopo do plano: **motor agnóstico de canal** (a `Messaging::Capabilities` virou a fonte — `bulk_outbound`: cloud/baileys/SMS sim, Instagram NÃO por política da Meta, site NÃO por física; `bulk_content`: template/variações/texto; canal novo vira uma linha no arquivo) e **PARAR multi-canal dentro da fase** (`Messaging::OptOut` único + `OptOutListener` — PRIMEIRO da lista do AsyncDispatcher, senão o próprio "PARAR" ainda gerava resposta de IA; o WhatsApp continua detectando no payload cru porque só lá `text.body` existe separado do clique de botão). Antes da fase, **relatório multi-caixa** respondendo a pergunta do Harvey: a IA **já funciona em todas as caixas** (zero código de WhatsApp nela; a prova §2.6.2 já tinha registrado o widget recebendo automação) — o que faltava era a tela expor o `ai_agent_inbox_ids` que backend/API/specs tinham desde a F3a; virou o seletor "todas/selecionadas" com rádio explícito (lista vazia = todas no backend; o rádio mata o vazio-silencioso). **T0 fechou o furo da suspensão em superfície pública, e ele era MAIOR que o review da F5 dizia:** a API pública de caixa não tinha guard NENHUM (nem conta suspensa) — lead de suspensa criava card e acordava a IA; e o gate real do widget é o `WebsiteTokenHelper` (a casca só serve iframe e JÁ barrava conta, só não via agência). `SuspensionGuard` único nas 4 superfícies. Campanha baileys: mín. 3 variações de copy (anti-ban), warm-up <7 dias no trigger (`Messaging::PairingAge` extraído do SendGateService), audiência exclui opt-out NA RESOLUÇÃO (no cloud é a única proteção — envio não passa por Message), `Campaigns::BulkDeliveryJob` espaçado (5s+jitter) criando Message pelo caminho normal → **gates da F2 valem de graça**; relatório v1 por `source_id` (status não separa enviada de adiada — a adiada fica no default `sent`, §2.6.3). Armadilha nova que custou um conserto: `deep_munge` transforma `[]` em nil E `permit(chave: [])` DROPA chave nil — sem normalizar no controller, "voltar pra todas as caixas" nunca limpava a allowlist. **Limpeza da conta 2 FEITA no container** (fuso nil, regras de teste off, `ai_agent_enabled` on). Roteiro consolidado de TODAS as provas pendentes em `.superpowers/sdd/merry-mixing-toast/roteiro-nivel4-consolidado.md`. **Falta (tudo na mão do Harvey): merge #32 + deploy; chave de IA → §3a.5/PARAR completo/§3b.4; SMTP → §5.5; flag `whatsapp_campaign` → §6.5.** Ressalva registrada: a F6 avançou sobre F3a/F3b sem prova nível 4 — decisão do Harvey.
- 2026-08-05 — **Fase 5 (comercial) mergeada e o CI ressuscitado de graça — cinco das seis fases do Motor na `main`.** PR #31 (`33c0f1b`, 10 commits): quota de IA ligada ao plano (assinatura vigente MANDA sempre — plano ilimitado não ressuscita o `custom_attributes` legado, decisão A/B travada por spec), alarme 80%/100% por e-mail com cooldown 24h por limiar em Redis dentro de rescue que nunca derruba a IA, suspensão de agência propagando (filhas e painel 401; `pending_payment` não propaga pras filhas mas bloqueia o painel), super admin com motivo de suspensão + aviso do Stripe, e a fonte de captação `POST /public/api/v1/inboxes/:id/leads` — idempotente por `external_id` no contato E na conversa, strong params escalar (hash no campo vira ausente, não 500), telefone mascarado normalizado (**lead nunca se perde por máscara**), throttle 60/h/token, mais a UI na tela da inbox API com URL, snippet de `<form>` e botão de lead de teste. **O CI parou no meio da sessão:** o Actions do repo privado foi bloqueado por billing e o Harvey decidiu não pagar — a saída foi um **espelho público só do código** (`hdev-crm-ci`, commit único com allowlist de diretórios), onde runner é grátis e ilimitado; o Ruby voltou a ter interpretador. **Incidente no caminho, registrado porque a lição vale:** o primeiro push saiu do repo principal e publicou o histórico do workspace (`_memoria/` incluso) — o `cd "$A" || cd "$B"` que passei é sintaxe de bash e o PowerShell não aceita `||`, então o `cd` falhou e o `git push` rodou de onde estava; repo deletado e recriado, **sem credencial exposta** (`.env` nunca esteve no git). Guarda que vale pra sempre: `git log --oneline -1` tem que mostrar o commit `sync:` antes de qualquer push do espelho. rspec veio **verde de primeira** nas duas rodadas (de novo); os vermelhos foram só rubocop — e um deles era **falso positivo do próprio espelho** (`Lint/ScriptPermission`: o tar no Windows perde o bit 100755; o script de sync agora restaura). Review final também mediu que o `ensure_current_account` tinha estourado a complexidade — extraído. **Furo achado pelo review e anotado como 1º item da F6: a suspensão de agência não alcança superfície pública (leads/widget), então agência suspensa continua gerando custo de IA.**
- 2026-08-04 (noite) — **Fase 3b mergeada: a IA entrou no fluxo do chatbot e o "dono IA" ficou visível.** PR #30 (`f2e5b2d`, 14 commits, CI 4/4 com rspec verde de primeira). O `AiNode` trocou `chat` puro por `Ai::ToolLoop` com as 5 tools da F3a — as duas portas de IA passaram a operar igual —, com o PARAR gateando ANTES do modelo e o handoff por tool saindo pelo handle `handoff` (decidido por `ToolLoop#executed`, nunca por reparse de texto). **O review final da branch pegou o buraco que os reviews por task não viam:** o nó entregava as 5 tools de escrita **sem o bloco de disciplina** do system prompt da F3a ("tool só sob demanda", "vocabulário interno nunca volta pro cliente") — ou seja, reabria o vazamento que a F3a tinha fechado; virou a constante compartilhada `TOOL_DISCIPLINE_PROMPT`. Do lado visível: `Conversation#ai_handling?` no payload e no `EventDataPresenter` (badge vivo por websocket), filtro `conversation_type=ai` extraído pra service próprio (o `ConversationFinder` ficou em ClassLength **175/175 exato** — próximo toque estoura), badge "IA atendendo" na lista e no header, aba "IA" que abre em "Todos" porque Minhas ∩ IA é vazia por construção. Dívida paga junto: `ai_node_spec` e `deal_node_spec` (primeira suíte `Chatbots::Nodes::*` do repo), ator "Agente IA" nas activities de etiqueta — e aqui o implementer corrigiu o *registro*: o sintoma pré-fix não era "ator vazio", era **nenhuma activity nascendo** (`return unless user_name`) — e o spec-guard automatizado de "nenhuma tool do `:agent` envia mensagem". Um exemplo de frontend foi provado por **mutação** pelo próprio reviewer (removeu o guard, o teste ficou vermelho, restaurou, verde). **Provas nível 4 (§3b.4) na mesma fila da F3a: dependem da `ANTHROPIC_API_KEY`.**
- 2026-08-04 — **Fase 3a (IA operadora) entregue, mergeada e verde: a IA deixou de só conversar e passou a operar.** PR #29 (`229a87b`, 7 commits) com o ciclo SDD completo — 4 tasks, review por task, 2 fix rounds, review final da branch + fix wave única (8/8 fechados, 0 Critical em nenhuma rodada). Cinco tools conversation-scoped vivas em `SETS[:agent]`, handoff barato por regex antes do LLM (custo zero) e o `AgentReplyService` rodando `ToolLoop`. **A tese de segurança resistiu ao ataque adversarial do review final:** nenhuma tool aceita id do modelo; onde texto do modelo vira chave de busca, a busca é escopada em `account.labels`/`deal.deal_pipeline.deal_stages` — e a varredura anti-id do registry virou allowlist recursiva (qualquer propriedade nova quebra o teste). Duas emendas do Harvey: ligar o AgentReplyService ao ToolLoop entra na fase (sem isso o §3a.5 era improvável), e `EtiquetarConversa` só aplica etiqueta já cadastrada (`add_labels` grava tagging e NÃO cria `Label` — a inventada nem aparecia no cadastro e furava a validação de formato). **Dois bugs de produção achados de carona:** o `default_scope` ASC do `Message` (PR #27) engolia o `.order(desc)` do `history_messages` — a IA lia a conversa INVERTIDA desde 03/08 (virou `reorder`, idem no `AiNode`); e o PARAR da F2 não gateava a escrita (`enabled_for?` ignorava `automation_opted_out` — com as tools, quem pediu PARAR teria dados reescritos sem receber nada). Contenções novas: teto de 3 negócios por conversa (o ciclo criar→perder→criar enchia o kanban da CONTA e disparava `deal.won` no barramento) e truncagem do vocabulário nos erros de tool. Lição de CI: os specs às cegas passaram todos — a única falha em 6257 foi o guard herdado que assertava `SETS[:agent]` vazio, o invariante que esta fase derruba de propósito (reescrito pro invariante novo, não deletado). **Falta a prova nível 4 (§3a.5) e o PARAR completo (§2.6.2): as duas dependem da mesma `ANTHROPIC_API_KEY` em produção.**
- 2026-08-03 (noite) — **Provas da F2 fechadas e Fase 4 inteira entregue e provada em produção no mesmo dia.** §2.6.1 throttle: chip de teste pareado no baileys local (QR via script ps1 — PowerShell 5.1 quebra .ps1 UTF-8 sem BOM com `>` em string; ASCII+BOM resolve), 5 POSTs concorrentes saíram serializados (mín. 1.13s, PASS). §2.6.2 PARAR parcial: baseline por regra de automação (substituiu o bot), "Parar" gerou nota de opt-out, automação retida (failed + nota), humano enviou, "vou parar mais tarde" não casou, idempotente — e o PARAR só existe no pipeline WhatsApp (widget do site não detecta, by design). §2.6.3 janela: fuso da conta → Hawaii forçou fora-de-janela, postpone SILENCIOSO by design (a prova é `antiban_reschedule_count`=1 + chegada sozinha na abertura — confirmada). PR #27 provado (backfill com horário real em ordem; eco ao vivo capturou até os throttle-tests). **Fase 4 (PR #28, 14 commits, merge `9309315`) rodou o ciclo SDD completo em uma sessão**: lost_reason obrigatório (modal no drag E no form de edição — o review final pegou o 2º caminho descoberto), vocabulary por funil (fix round matou default pt-BR hardcoded sendo persistido no jsonb), menu/título "Kanban" fixo com `vocabulary.deal` no botão "Novo {label}", motivo padrão "Movido por automação" em ActionService+DealNode. rspec às cegas verde de PRIMEIRA nas 2 rodadas; os vermelhos foram só rubocop (SkipsModelValidations em spec, CyclomaticComplexity — conserto por extract). schema.rb do artifact aplicado POR EDIÇÃO (cópia direta troca CRLF do checkout). Prova §4.4 integral: "Novo Oportunidade" + título "Kanban" na mesma tela, automação→Perdido sem quebrar. Limpezas feitas: fuso revertido pra nil; regras de teste "Teste 2"/"Teset - PErdido" a conferir. **Gate da F6: falta só PARAR completo (chave de IA). Próximo: F3a — worktree `feat/motor-fase3a-ia-operadora` já criado limpo na base `9309315`; a tool MoverNegocioDaConversa é o 3º call site do default_lost_reason (memória `motor-fase4-mergeada`).**
- 2026-08-03 — **Eco do celular fechado de ponta a ponta e o CRM ganhou horário real nas mensagens.** PR #11 mergeado na main depois da análise F2×eco (sem conflito de comportamento: os helpers da F2 usam `messages_data` que cobre `message_echoes`, o STOP tem guard `unless outgoing_echo` — ganhou teste de regressão novo — e o eco não dispara gates porque o `source_id` barra em `invalid_message?` ANTES do gate; os 3 conflitos reais eram de teste/infra: package.json→vitest da main, `inbound.test.ts` node:test→`inbound.spec.ts` vitest senão o job baileys colidia, specs add/add combinados em describes irmãos). Deploy dos 2 containers pelo Harvey e **backfill PROVADO em produção**: re-pareamento entregou 8 de 4.951 mensagens da janela de 24h. O sintoma que a prova revelou (mensagens com hora de "agora" e desordenadas) virou o **PR #27, mergeado**: `created_at` de mensagem WhatsApp entrante vem do timestamp real do payload (futuro clampa em agora) e o `default_scope` de `Message` desempata por `id`. **A lição da sessão saiu da caçada do PR #27** — um "flaky de suíte cheia" determinístico que passava isolado: 5 rounds de instrumentação no CI (dispatch de arquivo isolado → par de arquivos → TTL do lock → backtrace no `acquire!` → contador no cleanup) provaram que deletar chave DENTRO de `scan_each` do Redis pula chave quando o rehash encolhe a tabela (`deleted=7 still="true"` no log) — meus describes novos vazavam locks de dedupe e mudaram a geometria do keyspace, e o lock órfão de ~1s engolia a mensagem do exemplo vizinho sem erro. Conserto: cleanup nos describes novos + os 5 cleanups do projeto migrados pra coleta-antes-de-deletar (memória `rspec-redis-scan-delete-armadilha`). Micro-lições que custaram rounds: `Metrics/ModuleLength` conta código e não comentário; +1µs em `created_at` não sobrevive ao cast do Rails (banco recebe `.000000`). **Pendente: deploy do container Rails (só ele) pro PR #27 valer em produção.** Provas da F2 na fila: `baileys-service/scripts/throttle-test.mjs` criado pro §2.6.1 (precisa de chip pareado local); §2.6.2 tem prova parcial sem bot; §2.6.3 é roteiro de produção. Próxima fase à escolha do Harvey: F4 (kanban + rename) ou F3a (IA operadora)
- 2026-08-02 (noite) — **Deploy da Fase 2 confirmado no ar e sessão encerrada com pendências mapeadas.** O log do baileys provou a conexão saudável (versão WA em runtime, LID session, 812 pre-keys) com 2 erros benignos de JSON no backlog offline (upstream da lib, capturados por notificação, sem crash — se cliente reclamar de mensagem sumida do período offline, o rastro é esse). **Bug real reportado pelo Harvey: eco do celular** — mensagem enviada por ele DIRETO pelo WhatsApp não aparece no CRM; o conserto JÁ EXISTE no PR #11 (`feat/baileys-eco-confiabilidade`, verde desde 31/07, nunca mergeado) — mas a Fase 2 mexeu no incoming do Baileys depois disso, então a próxima sessão precisa analisar conflito real antes de mergear (+ deploy dos 2 containers + env `HISTORY_SYNC_MAX_AGE_HOURS` + re-parear pro backfill). **Provas da F2 adiadas com causa:** o teste do PARAR exige bot ativo e não há chave de IA configurada; o teste da janela idem. Diretriz nova do Harvey (salva em preferencias.md): teste automatizável → Claude escreve script e roda; teste manual → Harvey executa com roteiro. `count: 3` instâncias no boot do baileys com só 1 ativa — possível órfã de novo, conferir com o método do diário de 28/07. CLAUDE.md/estrategia atualizados pra "quatro jobs" no CI
- 2026-08-02 — **Fase 2 do Motor Integrado (anti-ban) mergeada na `main`** (PR #26, merge `7e8ab69`, 18 commits, CI verde nos 4 jobs — incluindo o job `baileys` NOVO, que estreou verde: o microserviço saiu do escuro com seus primeiros 19 testes). No mesmo dia, a **prova nível 4 da Fase 1 fechou** (regra de fábrica salvou pela UI, card nasceu sozinho, `deal.stage_changed`+`deal.won` no log do worker com payload GlobalID) e o Harvey decidiu o rename de rótulo "Negócios"→"Kanban" (§4.2b novo no plano, só I18n). A Fase 2 entregou: matriz `Messaging::Capabilities` (baileys é o único ban_risk), **emenda de plano decidida em execução** — opt-out virou coluna própria `contacts.automation_opted_out` porque a exploração provou que `blocked` é o mute global herdado (descarta TODO inbound; contato arrependido nunca se re-engajaria; mute/unmute de UMA conversa togglaria o opt-out global) — STOP detection ancorado SÓ em texto digitado (o review pegou que título de botão entrava no match: menu com "Cancelar" bloquearia o cliente no toque), `SendGateService` puro (opt-out → janela 7h-22h fuso da conta → warm-up 20/50/100/200/∞ aos 0/4/8/15/31 dias de pareamento → cap 300/dia) com enforcement no `SendOnWhatsappService` (postpone com jitter ≤15min pra não sincronizar a manada das 7h; teto 3 reagendamentos; retida/negada = `failed` + nota privada — veto nunca é silencioso). **Os reviews de opus pagaram a sessão**: pegaram por leitura 2 bugs de um token que matavam a feature inteira (`TimeZone[nil]` estoura em conta sem `reporting_timezone` — o `|| UTC` nunca rodava; e `paired_at` esperando `'open'` quando o microserviço só emite `'connected'` — todo número ficaria preso em 20 msgs/dia pra sempre), mais 2 armadilhas de factory nos specs (o `after(:build)` do `:message` engole `sender: nil`; `ensure_baileys_instance_config` backfilla o instance_id que o teste queria ausente). O rspec passou **verde de primeira** com ~90 exemplos escritos sem Ruby local; o lint custou 2 rodadas — a primeira por erro MEU de medição (grep com `-A3` truncou a lista de ofensas: eram 5, não 1 — ler sempre o bloco `Offenses:` inteiro), a segunda fechou com `process_messages` extraído (9→3) e o rename `increment!`→`record_send!` (o cop `Rails/SkipsModelValidations` casa o NOME do método em qualquer receiver). `db/schema.rb` veio do artifact do CI como manda o fluxo. **Pendente: deploy + níveis 3-4 da fase (§2.6) — e a Fase 6 só abre com a F2 provada em produção**
- 2026-08-01 — **Fase 1 do Motor Integrado mergeada na `main`** (PR #25, merge `712a9c5`, 7 commits, CI verde) — o plano de 6 fases que faz as peças (kanban, chatbot, Baileys, IA, automação) se enxergarem via barramento de eventos (`~/.claude/plans/merry-mixing-toast.md`). Entregue: o fix de 1 linha do `create_deal` (`noParamActions` do `validations.js` — era o bug de 27/07 que impedia salvar a regra do kanban pela UI), 7 eventos novos (`deal.created/stage_changed/won/lost`, `chatbot_flow.completed/aborted`, `whatsapp.connection_changed`) com dispatch no Deal (callbacks de activity que já existiam), ChatbotSession (`after_update_commit` novo) e `BaileysSessionService#write_state` (explícito + dedup, porque `update_column` pula callback e a aba Conexão faz polling), listener de automação pros 4 eventos de deal com i18n en/pt_BR, e ~500 linhas de specs. **O rspec passou verde de primeira** — specs Ruby escritos inteiramente sem interpretador local, na base de espelhar idiomas dos specs existentes; método: subagente implementa → subagente revisa com o diff num arquivo → fix loop → review final da branch com opus, que pegou 1 ofensa de rubocop e 2 gaps de cobertura ANTES do CI. Ainda assim custou 2 rodadas: a 1ª caiu em `lint` (2× `Layout/HashAlignment` — kwargs multi-linha alinham com a primeira CHAVE do hash, não com o primeiro argumento; reviewer tinha medido `ArgumentAlignment`, cop errado) e em `vitest` (`useAutomation.spec.js` exige que evento novo em `AUTOMATIONS` entre no `manifestCustomAttributes` ou na lista de exclusão do teste — os specs "vizinhos" do nível 1 não cobriam; **lição: nível 1 de JS é a suíte INTEIRA**, ~10 min; as 2 falhas locais de timezone `availabilityHelpers`/`filterHelpers` são ruído desta máquina GMT-3, verdes no CI). Semântica decidida e testada: `deal.won`/`lost` SUBSTITUI `stage_changed` na transição; deal criado direto em etapa won/lost emite só `deal.created`; listener de deal SEM guard `performed_by_automation?` de propósito (a cadeia regra-de-fábrica `conversation_created → create_deal → deal_created` precisa disparar). Descoberta: `automation_rule.rb` NÃO tem allowlist de `event_name` — o bullet do plano era vácuo. Ledger com os deferred minors pras fases 2-6 preservado em `.superpowers/sdd/merry-mixing-toast/progress.md` (git-ignored). **Pendente (gate do plano pra Fase 2): a prova nível 4 em produção** — rebuild, criar a regra "conversation_created → create_deal" pela UI, mensagem real → card no kanban, mover pra Ganho → `deal.won` no log (grep `EventDispatcherJob`)
- 2026-07-30 — **Fase 6: as 7 constantes internas Ruby renomeadas** (PR #7 `fase6/renames-internos`, 8 commits, CI verde: `5996 examples, 0 failures` em 16m03s; **mergeado em `5f4d3f9`** depois do Deploy 1 da Fase 3b ser confirmado no ar — a separação em dois deploys existia pra não misturar rename de constante com mudança de front). **Deployada e confirmada às 20:26** — `rails runner "puts HdevCrm.config[:version]"` respondeu `1.0.0`, ou seja o app boota como `HdevCrm::Application`, que é **o** teste da fase (renomeou o módulo da app e o rastreador de exceções: referência esquecida impediria o processo de subir). **Lição de método, do diagnóstico que quase deu errado:** logo após o merge o mesmo comando deu `uninitialized constant HdevCrm`, e a leitura certa era "imagem velha" — o merge foi 20:16:16 e o comando 20:17:07, 51s depois, sem rebuild no meio. Propus como controle rodar com a constante **velha** (`Chatwoot`), esperando que respondesse; ela falhou também, o que parecia derrubar a hipótese. Não derrubava: o rebuild entrou entre os dois comandos, então o controle rodou na imagem **nova**, onde `Chatwoot` corretamente já não existe. Ou seja, num alvo que muda sozinho, teste de controle expira — a sonda que não teria ambiguidade é `Rails.application.class`, que diz qual código está rodando em vez de perguntar se um nome existe. `ChatwootFbProvider`, `ChatwootDequeuedLogger`, `ChatwootMarkdownRenderer`, `ChatwootCaptcha`, `ChatwootExceptionTracker`, `ChatwootApp` e `module Chatwoot` → prefixo `Hdev`/`HdevCrm`. **Duas armadilhas de grep custaram dois runs vermelhos, e são erro de medição, não de código:** (1) `Chatwoot::` **não casa** `Chatwoot.` — o namespace expõe 4 métodos de módulo (`config`, `redis_ssl_verify_mode`, `encryption_configured?`, `mfa_enabled?`) usados em **30** lugares, e minha contagem inicial de "2 refs" era da string literal `module Chatwoot`, não da constante; o grep certo é `\bChatwoot[.:]`. (2) **filtrar por extensão esconde ERB dentro de YAML** — `config/cable.yml:6` tem `<%= Chatwoot.redis_ssl_verify_mode %>` lido pelo `config_for` do ActionCable, e nenhum grep restrito a `.rb`/`.erb` acha. As duas quebraram o **boot** (`NameError` no `db:create`), então o rspec morreu em ~90s sem rodar teste — barato só porque cada constante subiu sozinha e foi validada antes da próxima. Duas correções ao plano de 26/07: a pior armadilha (`Chatwoot::Application` em `config.ru`/`Rakefile`/`bin/*`/`environments/*`) **não existe neste repo** (usam `Rails.application` e `APP_PATH`), e duas constantes nem passam pelo Zeitwerk por viverem em `config/initializers`. Brindes: `def bot; Chatwoot::Bot; end` apontava pra constante inexistente e `bot` não faz parte da interface `Providers::Base` da gem (conferido na fonte) — código morto apagado; e `sent_from_chatwoot_app?`, que o grep da constante não pegava por ser nome de método. **Não mexido de propósito, porque o custo é de runtime e não de código:** `_chatwoot_session` (trocar desloga todos os agentes — e o rename do namespace **não** deslogou ninguém porque a chave é explícita, não derivada da classe da app) e `channel_prefix: "chatwoot_#{Rails.env}_action_cable"` (rompe assinaturas de ActionCable em voo)
- 2026-07-30 — **A `main` ficou vermelha por um spec que passava por sorte desde o baseline do fork** (PR #6, `fix/agent-summary-order`, verde: `5996 examples, 0 failures` em 14m48s — **aberto, aguardando merge**). O run do merge das Fases 3b+transcrição fechou com 1 falha em `agent_summary_builder_spec.rb:96`, sem relação com nenhum dos dois PRs (os dois passaram verdes separados). A falha era de **ordem do array**: o `got[0]` era o `expected[1]`, os dois agentes trocados de posição. Causa: `prepare_report` fazia `account.account_users.map` **sem `ORDER BY`**, e o spec compara o array inteiro com `eq` — sem ordenação o Postgres devolve na ordem que quiser (ordem de heap, que muda com escrita/vacuum/plano). O spec não era tocado desde `d534e7d`, o baseline: veio assim do upstream e nunca tinha caído. **Não era só teste instável** — o mesmo builder alimenta `GET /api/v2/accounts/:id/summary_reports` e o CSV do `download_report.rake`, então a lista de agentes podia trocar de posição entre dois carregamentos da tela e duas exportações do mesmo período saíam em ordens diferentes; ninguém tinha percebido em produção. Consertado com `order(:user_id)`, que resolve os dois lados sem tocar no spec (verificado: `AccountUser` não tem `default_scope`, o `summary_reports_controller_spec` stuba o builder, e é o único builder do repo com esse padrão). **Duas armadilhas de leitura de CI, nesta mesma sessão, e nenhuma confiável isolada:** (1) `gh run watch --exit-status` devolveu **exit 0 para um run cancelado** — código de saída não prova verde, tem que ler o `conclusion`; (2) o `cancel-in-progress: true` fez um push de memória (só markdown) **cancelar o run do merge** que estava em andamento. A prova de flakiness que fecha o caso: **rerun do mesmo commit, sem nenhuma alteração, passou** — um run vermelho e um verde no mesmo SHA
- 2026-07-30 — **Fase 3b executada e mergeada** (PR #5, `76eddb7`, CI verde nos três jobs: `5975 examples, 0 failures, 64 pending` em 16m42s). Junto, o PR #4 (transcrição de áudio) mergeado em `0c7ec8a`. **O bloqueio que a memória registrava não existia** — segundo caso seguido, depois do "relay de VAPID" da Fase 3-tele. A fase estava marcada como travada até as páginas de termos existirem no hdev.online, mas `TERMS_URL`/`PRIVACY_URL` são chaves de `installation_configs` e o `Agency#global_config_overrides` já as sobrescreve **por agência**: o mecanismo de cada revendedora apontar pros próprios termos estava pronto o tempo todo, e o Help Center do próprio produto (`/hc/:slug/:locale/articles/:slug`, MIT, público, zero código) hospeda sem depender do hdev.online. **O achado real foi um bug de white-label que nós mesmos criamos:** o `Form.vue` aplicava a config por substituição de string literal (`.replace('https://www.chatwoot.com/terms', …)`), e a tradução pt-BR de 27/07 cravou `hdev.online` no texto de `en` e `pt_BR` — o replace parou de casar e **`TERMS_URL` virou no-op justamente nos dois locales que usamos**, então toda agência revendedora exibia os termos da HDEV no próprio cadastro. Virou interpolação do vue-i18n nos 57 locales, com spec que lê os 57 arquivos e falha se algum voltar a cravar URL (verificado revertendo um locale: falha só nele). Saiu junto a cadeia `feature_help_urls` → `vueapp.html.erb` → `window.globalConfig.helpUrls`, que publicava as URLs de documentação do Chatwoot em toda página e **não tinha nenhum consumidor no frontend** — deletada inteira, junto com os 12 `help_url` do `features.yml` (só as sub-chaves; a ordem do bitset não foi tocada). **Lição que custou um run de CI:** o fallback de remetente tinha DUAS dependências em spec — a fixture `notification.eml` do `reply_mailbox_spec` (antecipada, porque não stuba a env) e o `confirmation_instructions_spec:20`, que só apareceu vermelho no CI porque o grep por `accounts@chatwoot` rodou **antes** da decisão de mexer no `devise.rb`. Critério de escopo que vale repetir: links atrás de `isOnChatwootCloud` e `showOnCustomBrandedInstance` ficaram de fora — o fork força `isACustomBrandedInstance: () => true` e `deploymentEnv` nunca é `cloud`, então não renderizam (mesma lógica que deixou as classes `woot-*` fora na Fase 5). **Achado que não é link e vale mais que a fase:** o **Captain tem rota registrada no front (`captainRoutes` em `dashboard.routes.js`) mas zero model no backend desde a Fase 3** — se aquela tela abrir, dá 500. Também confirmado que a branch `fix/baileys-wa-version` nunca rodou CI (o workflow só dispara em `main`/PR/dispatch e cobre `hdevCRM/`, não o microserviço); `tsc --noEmit` passa limpo nela
- 2026-07-29 — **WhatsApp fora do ar com `code_405` e a lição de não fixar versão** (branch `fix/baileys-wa-version`, commit `16e5480` pushado, sem PR ainda). O painel mostrava "Conectando… code_405" e o container reconectava de 60 em 60 segundos a noite toda. Duas pistas separaram o diagnóstico do palpite: o log dizia `"username":"5516997223968"` + `logging in...`, ou seja **relogin com credencial salva**, não pareamento — e o Baileys **só emite QR quando `creds.registered` é false**, então credencial morta no disco é um beco sem saída pelo painel (o botão "Gerar QR code" recarrega a mesma credencial e volta pro mesmo erro). Depois de apagar a auth state pelo endpoint de logout, o log virou `passive:false` + `devicePairingData` + `not logged in, attempting registration...` e **levou o mesmo 405** — o que matou a hipótese de sessão invalidada e apontou pro handshake. Causa: a **versão do cliente WhatsApp Web que o Baileys hardcoda** (`lib/Defaults/index.js`) — a rc13 pede `2.3000.1035194821`, o WhatsApp já exige `2.3000.1044104838`. Provado em A/B na mesma máquina, um atrás do outro: velha → `CLOSE statusCode=405`, nova → `QR OK`. **É o mesmo bug do `5af436b`**, que "resolveu" subindo 6.x → rc13 e durou dois meses; a rc14 compraria mais um mês, então a versão passou a ser buscada em runtime (`fetchLatestWaWebVersion`, timeout 5s, fallback pra última boa da réplica). Junto, dois consertos que o 405 escancarou: 405/403/411 entraram no conjunto de fechamentos fatais ao lado do 401 (apagam a auth state e **param** — antes o serviço martelava o WhatsApp para sempre com credencial morta, risco de ban de número não-oficial; 428/408/515 seguem com backoff) e o `teardownSocket()` foi pra antes do `removeAuthState()` removendo também o listener de `creds.update`, que reescrevia o `creds.json` em cima do diretório recém-apagado. Descobertas de ambiente: o `baileys-service` **roda e pareia desta máquina** (provision→connect→QR em ~2s, sem Ruby/Docker), e o container é alpine **sem `curl`** — a API interna se chama com `node -e fetch`. Pendente: rebuild do container `baileys` (imagem própria, o rebuild do app não cobre) e repareamento do `+5516997223968`, cuja sessão foi apagada no diagnóstico
- 2026-07-29 — **Fase 3-tele: telemetria e o hub do Chatwoot cortados** (PR #3, mergeado em `59025f0`, CI verde nos três jobs: `5975 examples, 0 failures, 64 pending` em 11min21s, rubocop+eslint `2142 files, no offenses`, vitest 382 arquivos). 107 arquivos, −1.401 linhas; nenhuma linha do app fala com `hub.2.chatwoot.com`. **A pendência que a memória dava como bloqueio não existia:** a nota dizia que os serviços de push usavam o hub como "relay de VAPID" e precisariam de substituto — era **FCM**, não VAPID. Web push sempre foi local (`WebPush.payload_send` com as chaves do `VapidService`); o relay só valia pro push do app mobile e o substituto (`send_fcm_push` com credencial Firebase própria) já estava no mesmo arquivo. O que vazava: título e corpo da notificação iam pro servidor deles quando a instância não tinha Firebase. Saiu junto: `register_instance` no onboarding (empresa/nome/e-mail do dono) + o checkbox que o disparava, os dois jobs de version check (o `TriggerDailyScheduledItemsJob` **só existia** pra agendar o outro), a chave Redis `LATEST_CHATWOOT_VERSION`, o campo `latest_chatwoot_version` da API + swagger, a action `refresh` do super admin, `UpdateBanner`+`versionCheckHelper` (chave `UPDATE_CHATWOOT` fora dos 57 locales), o changelog do sidebar e os testimonials do signup. Dois achados de brinde: `'saml'` no `allowed_login_methods` era código morto desde a Fase 3 (dependia de `pricing_plan != 'community'`, que virou constante), e `REST_CLIENT_EXCEPTIONS` ficou sem consumidor — a constante saiu, a gem `rest-client` não (precisa de bundler pro lock). Armadilha nova: os 5 JSONs de swagger têm que ser editados **por texto** — `JSON.parse`+`stringify` reordena as chaves numéricas de status HTTP e reescreve o arquivo inteiro. `DISABLE_TELEMETRY` e `ENABLE_PUSH_RELAY_SERVER` viraram no-op e saíram do compose e do doc. Confirmado que as 18 falhas de vitest da máquina local **passam no CI** — são de data/fuso, não de código
- 2026-07-29 — Fase 3 do de-chatwoot executada e mergeada: `enterprise/` e `spec/enterprise/` deletados (733 arquivos, −51.915 linhas), PR #2 em `c4e3f74` com CI verde e a suíte rodando **inteira** pela primeira vez, sem `--exclude-pattern` (`5989 examples, 0 failures`). O probe do passo 1 pagou o run: fechou vermelho com 9 falhas do mesmo override — `config/application.rb` prependava `enterprise/app/views` **sem guarda nenhuma**, dependendo do arquivo existir no disco e não da env var, então o `confirmation_instructions.html.erb` do enterprise chamava `account.saml_enabled?` com o módulo desligado. Lição: view path do enterprise não era gateado, só o módulo era. Correções ao que estava registrado: `CaptainFeaturable` e `AccountCaptainAutoResolve` **ficam** (são MIT puros), e a Fase 3.5 virou lista de espera com gatilho por pedido de cliente — as duas reconstruções que valem mais são transcrição de áudio (~60 linhas, contrato já MIT no core) e os campos de limite/feature do super admin (~20 linhas, é a mecânica dos planos de revenda)
- 2026-07-28 — CI destravado e a suíte rspec **terminou pela primeira vez** (18min19s, 8028 exemplos). O "travamento eterno" (3 runs de 90 min mudos) era o autoBuild do Vite disparando dentro do primeiro spec de `/widget` num job sem Node — o vite_ruby captura a saída, então nada aparecia no log; achado com o `workflow_dispatch --format documentation` que já estava rodando desde 17:42. Atrás dele, mais dois: OOM de heap no build (default ~2 GB; produção usa 4096) e o `ConfigLoader` rodando de carona no `db:migrate` (`db_enhancements.rake`), semeando ~106 `installation_configs` no banco de teste — a linha semeada vence o stub de ENV em `GlobalConfigService.load` e derrubou ~70 exemplos em cascata (signup 404, HUMAN_AGENT, UniqueViolation). PR #1 (`ci/rspec-vite-autobuild-hang`): pnpm+build explícito+stdin `/dev/null`, TRUNCATE pós-migrate, `spec/enterprise` excluído (239 das 340 falhas; licença proíbe o uso, Fase 3 deleta), super admin com `I18n.default_locale` em vez de pt_BR cravado (specs assertam inglês) e 3 assertions presas no branding antigo. Análise completa das 340 falhas com clusters e causas em `~/.claude/plans/fa-a-isso-e-use-effervescent-corbato.md`. **Desfecho na mesma noite: 340 → 2 → 0.** O TRUNCATE + exclusão do enterprise derrubaram tudo menos 2 specs desatualizados (marcador de brand virou `--blue-9/10/11` no redesign de 26/07; nota privada de handoff é `outgoing` no default do MessageBuilder) — zero regressões reais de código. PR #1 mergeado (`f0fb6c6`) com o primeiro CI 100% verde do repo: `5994 examples, 0 failures`, rspec em ~15-16 min. Mapa visual do diagnóstico (graphify, 55 nós/8 comunidades) publicado como Artifact "Anatomia das 340 falhas"
- 2026-07-28 — Aba "Conexão" da inbox Baileys travava a **página inteira** em "carregando" — e só quando a sessão estava conectada, que é por isso que passou batido nos testes anteriores. Não era o microserviço: o log prova que a instância do `+5516997223968` (`2d3f0585-…`) fez login, subiu pre-keys e já recebia mensagens. Era ciclo fechado no frontend, entre três arquivos: o `BaileysSession` emitia `connected` já na primeira leitura do `onMounted`, o `Settings.vue` respondia com `dispatch('inboxes/get')`, o `uiFlags.isFetching` trocava a **raiz** do template pelo `<SpinnerLoader>`, o componente desmontava, remontava com o `status` zerado — e a primeira leitura acontecia de novo, duas XHRs por volta, para sempre. Corrigido com /ponytail (guard, sem abstração nova): `status` nasce `null` — que não é o mesmo que `'disconnected'` — e o emit exige transição observada dentro da mesma montagem; `baileysStatusLabelKey(null)`/`baileysStatusClass(null)` já caíam nos mesmos defaults, então a primeira pintura não mudou. Spec de regressão em vitest, verificada revertendo o guard (falha com `Expected undefined / Received [[]]`). Confirmado que nenhum outro caller dispara `inboxes/get` de dentro da página — os demais são ação do usuário, one-shot. No microserviço, o problema irmão: `loadAll` reconectava **toda** instância persistida, então as órfãs geravam QR eterno (`QR refs attempts ended` → reconecta → repete) no mesmo event loop single-thread das boas; agora só reconecta quem tem credencial pareada, testando `creds.registered` e **não** a existência do `creds.json` (o `useMultiFileAuthState` grava esse arquivo já na primeira tentativa, com `registered: false`, então existir não prova nada) — é o que o comentário da função prometia e o código não fazia. As 3 zumbis (`0ed2d284-…`, `5716d36c-…`, `685e21f5-…`, de canais deletados) foram apagadas do volume pelo Harvey; corrigida na memória a anotação que dava a `5716d36c-…` como o canal legítimo — segui-la teria apagado a sessão viva. Origem das órfãs: `teardown_baileys_instance` faz `rescue StandardError` e só loga (de propósito, `before_destroy` não pode barrar o delete), então microserviço fora do ar na hora de apagar a inbox deixa diretório órfão pra sempre. vitest 2/2, eslint sem erro novo, `tsc --noEmit` limpo. Não corrigido, de propósito: o controller Rails só resgata `ApiError`, então `Net::ReadTimeout`/`ECONNREFUSED` viram 500 (ruído no rastreio, não causa do travamento). Falta rebuild pro fix do frontend valer em produção
- 2026-07-28 — Validação do deploy (bloco A) e Fase 5 antecipada. As 9 migrations rodaram em produção (`needs_migration?` → `false`, 8 tabelas de chatbot/deal); o terceiro check voltou `{}` — não havia **nenhum** `Channel::WebWidget` no banco, então o backfill de cor não tocou em nada, e é isso que autorizou renomear a superfície JS sem retrocompatibilidade. A tela "sua caixa de entrada está pronta" expôs o que faltava: o snippet que a agência cola no site do cliente dela ainda dizia `window.chatwootSDK`. Renomeado com /ponytail (rename mecânico, zero abstração nova): `chatwootSDK`→`hdevSDK`, `$chatwoot`→`$hdev` (66 refs), `chatwootSettings`, `chatwootWebChannel`/`PubsubToken`, prefixo postMessage `chatwoot-widget:`→`hdev-widget:`, eventos `chatwoot:*`→`hdev:*`, as 7 constantes `CHATWOOT_*` do sdkEvents, id do DOM, chaves de localStorage e `chatwoot_bot.png`→`hdev_bot.png` — 49 arquivos + 57 locales. Fora de escopo por decisão: classes `woot-*` (617 refs) e cookies `cw_*`, que não soletram "chatwoot". Três armadilhas confirmadas no código: o `WOOT_PREFIX` só era usado pra LER (as escritas eram literais hardcoded nos dois lados do iframe — trocar só a constante quebraria em silêncio), `sed 's/$chatwoot/'` não casa nada porque `$` é âncora de regex, e `s/CHATWOOT_/HDEV_/g` teria destruído o `CHATWOOT_INBOX_HMAC_KEY` que o Ruby usa pra assinar. O grep de rede pegou dois que o mapa perdeu: `suspended/Index.vue` escutava `chatwoot:on-message` do widget de suporte do próprio dashboard, e `ContactNoteItem.vue` apontava pro PNG movido. 3.762 vitest verdes (as 18 falhas em 6 arquivos são pré-existentes e dependentes de data — confirmado rodando com tudo no stash). Achados não corrigidos: `Website.vue:21` crava `#009CE0` e sempre envia, então inbox de site criada pela UI nasce azul ignorando o default verde da coluna; `Label` ainda nasce `#1f93ff`. Falta rebuild + teste em `/widget_tests` — o SDK não tem nenhum teste unitário
- 2026-07-27 — Conexão do WhatsApp (Baileys) ficou visível e reconectável sem recriar a caixa de entrada. Causa raiz: a aba `configuration` é liberada por `isAWhatsAppCloudChannel`, então o `<BaileysSession>` que já existia no `ConfigurationPage.vue` era código inalcançável — o QR só aparecia dentro do wizard de criação. Criada aba própria **"Conexão"** (`whatsapp-session`) e removido o bloco órfão (uma superfície só). O `BaileysSession.vue` agora faz polling permanente (3s parear / 15s repouso — antes parava ao conectar e o selo congelava), mostra o número pareado e a hora da última verificação, e ganhou "Gerar novo QR code" (logout + connect com confirmação). Backend: `connect!` provisiona antes de conectar (`provisionInstance` é idempotente, então instância perdida em restart sem volume se auto-cura) e 404 do microserviço virou `not_provisioned` em vez de 503 "serviço fora do ar" sem botão que resolvesse; `connection_state` + timestamp passaram a ser serializados pra todos os papéis (sem `instance_id`/`webhook_secret`), alimentando o selo na lista de inboxes e o banner acima do campo de resposta. Sem aviso por e-mail de propósito — `prompt_reauthorization!` dispara o `whatsapp_disconnect` mailer e o Baileys oscila demais. Specs escritas (session service, controller, helper JS) mas não executadas: sem ruby/docker na máquina
- 2026-07-27 — IA própria (commit `18e879b`, pushado): análise mostrou que ~70% já existia em MIT (cliente Anthropic com quota, agente com handoff, motor de fluxo de 12 nós) e que a lacuna era uma só — a IA conversava mas não agia. Construída a fundação de tool calling: `Ai::Tool` (schema JSON Schema puro, neutro de provider), `Ai::ToolLoop` (loop agêntico contra 4 seams, teto de 8 iterações, quota e AiUsageEvent dentro do raw_chat que roda por iteração — é o que impede tool calling de furar o limite), `Ai::ToolRegistry` (`:agent` × `:copilot` como fronteira de segurança, não categoria) e `Ai::Tools::CreateChatbotFlow` (gera fluxo como rascunho; o FlowValidator existente é a rede). DEFAULT_MODEL Opus 4.8 → Haiku 4.5 (5x mais barato) e chaves Anthropic/OpenAI/Gemini no super admin. Escolhido loop manual em vez do `tool_runner` do SDK porque o helper exige DSL acoplada à Anthropic, incompatível com o multi-provider aprovado — o que também salvou `lib/llm/` da deleção. Descoberto que `Agency#global_config_overrides` é hash de branding, não store livre (override de chave por agência descartado). Nada executado: sem Ruby nem Docker na máquina. Pendente: backup do Postgres, deploy (roda as 9 migrations sozinho), regenerar schema.rb no container e rodar os specs
- 2026-07-27 — Tradução pt-BR completa do app (199 arquivos, 7 fases com 6 subagentes): locale padrão pt_BR no código (specs em :en via test.rb), rails.pt_BR.yml + secure_password.pt_BR.yml (o "Senha must contain..." do print virou pt-BR), flash de erro do super admin em vermelho (ia como notice verde), ~80 validações de models e ~140 erros da API pra I18n (en byte-idêntico pros specs), 20 subjects + ~30 templates de e-mail (convite de agente enterprise incluso — é o que renderiza), installation_config.yml (89) + features.yml (61) traduzidos, cauda do frontend (preChat por locale da conta, links termos → hdev.online, {min}), remetente-fallback sac@hdev.online. Pendente: precompile+specs no Docker, teste visual, criar páginas de termos/privacidade
- 2026-07-27 — Redesign do console super admin (commits 426385d + b0e0ed5, pushed): dark mode global tokenizando o SCSS do administrate (var(--sa-*) ponte pro bundle Vite — rgb(var()) direto quebrava o SassCompressor do precompile, 1º deploy falhou e o fix subiu em b0e0ed5), tradução que faltava (helpers.label completo, "Novo(a) conta", filtros), switch pill compacto de tema, sidebar/tabelas/forms no visual do app (tokens n-*, white-label por agência) e azul Chatwoot rgb(31,147,255) removido do gráfico do Painel. Skill Impeccable instalada (detector de design via hook) + PRODUCT.md e DESIGN.md na raiz como fonte de verdade visual. Falta: rodar o deploy com b0e0ed5 e testar claro/escuro página a página
- 2026-07-27 — Super admin de-Chatwoot + pt-BR completo: removido o widget de suporte do Chatwoot que mandava installation ID/e-mail pro hub deles (_javascript.html.erb), versão própria 1.0.0 (app.yml, package.json renomeado @hdev/hdev-crm, VERSION_CW), tela Configurações reescrita (sem identifier/plan/Discord/cards EE com upgrade — help vira mailto contato@hdev.online), features.yml sem seção premium e traduzido, tradução pt-BR das 27 views ERB + Vue do dashboard + locale administrate.pt_BR.yml novo com around_action forçando pt_BR no namespace, ícone de maleta pro menu Agências (chave :agencies faltava no sidebar_icons) e toggle claro/escuro com localStorage. 28 arquivos + 2 deletados. Sintaxe validada (prism/js-yaml), falta rebuild
- 2026-07-27 — "Editar fluxo" que não abria: era deploy + cache (502 do proxy durante redeploy + chunk JS de build antigo no navegador; hard refresh resolveu, sem bug de código). Depois, polish UX do canvas do chatbot estilo Make: duplo-clique abre config, node-drag-threshold=4 (clique com micro-arrasto ainda seleciona), snap-to-grid 20px, arestas animadas, busca na paleta, handles 14px com alvo ~30px (WCAG) e fix do fit-view brigando com o viewport salvo — 8 arquivos (FlowBuilder/FlowNode/i18n), sem commit
- 2026-07-27 — Diagnóstico completo Baileys+chatbot (~30 achados, dois agentes de varredura) e conserto dos críticos no commit 9d16269 (pushed): JID @lid resolvido pro número real (era a causa do "conectou mas não recebia"), logs em todos os descartes silenciosos, 401 do webhook logado com motivo, e 6 bugs quebra-tudo do chatbot (botões crashavam sessão via Hash puro no MessageBuilder, ResumeJob com lock_version que nunca batia, roteamento edges.first, re-trigger, handoff em retries/end). WhatsApp validado ponta a ponta em produção com chip real (recebe + envia). Identificadas 2 instâncias zumbis pra limpar no volume. Kanban vazio explicado: nada cria deal sozinho + bug de validação impede salvar a regra. Plano da rodada 2 aprovado (UX erros amigáveis, aba de reconexão, teardown com retry, regra de fábrica do kanban, rename Kanban) — não implementado
- 2026-07-27 — Entrega grande (plano aprovado, ainda sem commit): login do Super Admin no split-screen das telas de auth (+ dark mode do pack superadmin consertado), pt-BR zerado (calls.json, CAPTAIN_GENERATION, fallbackLocale nos 3 entrypoints), microserviço baileys-service (WhatsApp não-oficial: QR/pairing, proxy por instância, HMAC, anti-ban) integrado como provider 'baileys' do Channel::Whatsapp com disclaimer obrigatório na UI, kanban de Negócios (Deal/Pipeline/Stage + board vuedraggable + ações create_deal/move_deal_stage na automação) e chatbot com canvas visual (@vue-flow, 12 nós, motor com sessões/timeout/handoff). Relatório de análise em _memoria/analise-2026-07.md. Falta: commit, migrate+lint em ambiente com Ruby, envs no EasyPanel, rebuild, teste com chip descartável
- 2026-07-26 — Redesenhadas as 5 telas de auth com layout split-screen próprio (AuthSplitLayout.vue: painel escuro com brilho radial em CSS, headline usando o installationName em runtime, formulário em card à direita), textos em pt_BR/en. Dois bugs de white-label anteriores corrigidos junto: a cor da agência sumia no tema escuro (`:root` perdia pro `.dark` redeclarado num descendente) e o `brand_rgb` só escurecia, jogando o texto de acento pra 2,5:1 no card escuro — agora aceita percentual negativo e mede 6,0:1. Regra de acento registrada no design-guide. Falta rebuild no EasyPanel e DEFAULT_LOCALE=pt_BR
- 2026-07-26 — Diagnosticado e corrigido o 500 do /super_admin: não era o DISABLE_ENTERPRISE, era a sidebar gerando link de index pro recurso agency_users (que só tem new/create/show/destroy); adicionado à lista de exclusão da navegação. Pendente: rebuild no EasyPanel pra confirmar
- 2026-07-26 — Instância no ar no EasyPanel (o container web estava parado; rota do domínio apontada pra web:3000). Auditoria completa achou o job enterprise que revertia o rebrand todo dia + telemetria pro hub do Chatwoot; apurada a licença (core MIT vendável, `enterprise/` proíbe revenda) e decidido ir pra modo Community. Plano de 6 fases aprovado. Executado: paleta verde + 30 favicons + /swagger fechado (commit 5d1208e), kill-switch DISABLE_ENTERPRISE e marca restaurada no banco. Pendente: erro 500 no /super_admin
- 2026-07-23 — /instalar (perfil solopreneur/dev, HDEV CRM white-label pra agências); rebrand Chatwoot→Hdev CRM no fork (2.340 strings de UI/e-mails, views, config, e-mails de suporte sac@/contato@hdev.online); git instalado + repo iniciado (4 commits); artefatos de deploy EasyPanel prontos (docker-compose.easypanel.yaml + INSTALAR-EASYPANEL.md)
- 2026-07-20 — Template HDEV v1.2.0 finalizado e publicado no GitHub (regras em _sistema/, trava de segredos, scripts prontos, /atualizar-sistema)
